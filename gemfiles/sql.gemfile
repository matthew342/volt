# Contains gems needed for testing with sql adapter

source 'http://rubygems.org'

gemspec path: '../'

eval(File.read('gemfiles/common.rb'))

# volt-mongo gem for testing
 gem 'volt-sql'

# Database drivers for volt-sql
gem 'sqlite3'
gem 'pg', '~> 0.18.2'
gem 'pg_json', '~> 0.1.29'
